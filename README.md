# nim
A Reinforcement Learning algorithm for a game called Nim, where players take turns removing objects from piles. The person forced to take the last object loses. The rules:
1. One can choose exactly one pile to remove items from at a time.
2. One can remove one, more, or all items from a pile at a time.
3. One can remove items from a pile in any order.
